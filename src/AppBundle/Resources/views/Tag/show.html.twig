{% extends '::base.html.twig' %}

{% block body %}
    {% if tag.posts is empty %}
        <h2>
            Sorry, there are no posts associated with tag <span class="text-danger">{{ tag.name }}</span>
        </h2>
    {% endif %}
    {% for post in tag.posts %}
        <h2>
            <a href="{{ path('show_post', {'slug': post.slug}) }}">
                {{ post.title }}
            </a>
        </h2>
    {% endfor %}
{% endblock %}